build/
out/
*.wav
*.flac
.DS_Store
.cache/

# Task 4 calibration renders (~180 MB of WAVs, regenerated by tools/calibrate
# + tools/analyze_calibration.py) -- but calib/calibration.json itself is
# small (~2 KB) and IS committed, via an explicit `git add -f`.
calib/
